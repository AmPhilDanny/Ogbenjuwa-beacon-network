{
  "functions": {
    "api/index.ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "outputDirectory": "admin/dist",
  "buildCommand": "bun run build:admin && bun run build:server",
  "installCommand": "bun install",
  "framework": null
}
